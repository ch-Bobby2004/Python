import numpy as np
a = np.array([1, 2, 3])
b = np.array([4, 5, 6])
print(a + b)

a = np.array([1, 2, 3])
print(np.mean(a))   # average
print(np.max(a))    # maximum
print(np.sum(a))   # sum

arr = np.array([[1, 2], [3, 4]])
print(arr)

rr = np.array([[1, 2], [3, 4]])

print(np.mean(rr))   # 2.5
print(np.max(rr))   # 4
print(np.sum(rr))    # 10
